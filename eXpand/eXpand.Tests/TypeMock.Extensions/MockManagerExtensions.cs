using System;

namespace TypeMock.Extensions
{
    public static class MockManagerExtensions
    {
        public static void CalledCounter(Action action)
        {
//            MockManager.CalledCounter(action.Invoke())
//                Recorder<int> recorder = new Recorder<int>();

        }
    }
}
