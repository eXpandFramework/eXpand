namespace Xpand.Persistent.Base.ModelArtifact {
    public enum ActionState {
        Default,
        Disabled,
        Hidden,
        Executed,
        ExecutedAndDisable,
        ForceActive
    }
}