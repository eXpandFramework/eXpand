using DevExpress.Persistent.BaseImpl;
using DevExpress.Xpo;
using Xpand.Persistent.Base.PivotChart;

namespace Xpand.Persistent.BaseImpl.PivotChart {
    public class PivotOptionsBehavior : BaseObject, IPivotOptionsBehavior {
        public PivotOptionsBehavior(Session session) : base(session) {
        }
    }
}